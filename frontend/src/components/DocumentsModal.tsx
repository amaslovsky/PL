"use client";

/**
 * Modal listing the signed-in user's saved drafts. Not mounted anywhere
 * yet — reserved for a future ticket that surfaces /my-documents as a
 * dialog from any page. Triggers CSS-only, no portal wiring today.
 */
export function DocumentsModal() {
  return null;
}