"use client";

/**
 * Modal-based auth shell. Not mounted anywhere yet — reserved for a
 * future ticket that lets users sign in / sign up from anywhere without
 * redirecting to /login. Triggers CSS-only, no portal wiring today.
 */
export function AuthModal() {
  return null;
}