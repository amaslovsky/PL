services:
  web:
    build: .
    image: pl-app:dev
    container_name: pl-web
    ports:
      - "8000:8000"
    environment:
      - JWT_SECRET=${JWT_SECRET:-dev-only-jwt-secret-replace-in-prod}
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
    restart: unless-stopped

volumes: {}